import React from "react";
import Admin from "@/components/Admin";

type Props = {};
const page = (props: Props) => {
    return (
        <div>
            <Admin />
        </div>
    );
};

export default page;
